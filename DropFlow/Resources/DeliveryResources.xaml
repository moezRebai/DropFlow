<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ═══════════════════════════════════════════════════════════════
         COULEURS DES STATUTS
         ═══════════════════════════════════════════════════════════════ -->
    
    <!-- Planifié - Bleu -->
    <SolidColorBrush x:Key="DeliveryStatusPlannedBrush" Color="#2196F3" />
    <SolidColorBrush x:Key="DeliveryStatusPlannedLightBrush" Color="#E3F2FD" />
    
    <!-- Confirmé - Orange -->
    <SolidColorBrush x:Key="DeliveryStatusConfirmedBrush" Color="#FF9800" />
    <SolidColorBrush x:Key="DeliveryStatusConfirmedLightBrush" Color="#FFF3E0" />
    
    <!-- Annulé - Rouge -->
    <SolidColorBrush x:Key="DeliveryStatusCanceledBrush" Color="#F44336" />
    <SolidColorBrush x:Key="DeliveryStatusCanceledLightBrush" Color="#FFEBEE" />
    
    <!-- Terminé - Vert -->
    <SolidColorBrush x:Key="DeliveryStatusDoneBrush" Color="#4CAF50" />
    <SolidColorBrush x:Key="DeliveryStatusDoneLightBrush" Color="#E8F5E9" />
    
    <!-- Default -->
    <SolidColorBrush x:Key="DeliveryStatusDefaultBrush" Color="#9E9E9E" />

    <!-- ═══════════════════════════════════════════════════════════════
         STYLES PERSONNALISÉS
         ═══════════════════════════════════════════════════════════════ -->
    
    <!-- Style pour les cards statistiques -->
    <Style x:Key="StatCardStyle" TargetType="Border">
        <Setter Property="Padding" Value="20,16" />
        <Setter Property="CornerRadius" Value="12" />
        <Setter Property="Background" Value="{DynamicResource MaterialDesignCardBackground}" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect ShadowDepth="4" 
                                          BlurRadius="12" 
                                          Opacity="0.3" 
                                          Color="Black" />
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Style pour les boutons d'action dans les cards -->
    <Style x:Key="CardActionButtonStyle" TargetType="Button" 
           BasedOn="{StaticResource MaterialDesignToolButton}">
        <Setter Property="Margin" Value="4,0" />
        <Setter Property="Width" Value="32" />
        <Setter Property="Height" Value="32" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource PrimaryHueLightBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Style pour les badges de statut -->
    <Style x:Key="StatusBadgeStyle" TargetType="Border">
        <Setter Property="CornerRadius" Value="12" />
        <Setter Property="Padding" Value="10,4" />
        <Setter Property="HorizontalAlignment" Value="Center" />
    </Style>

    <!-- Style pour les colonnes Kanban -->
    <Style x:Key="KanbanColumnStyle" TargetType="Border">
        <Setter Property="CornerRadius" Value="12" />
        <Setter Property="Padding" Value="12" />
        <Setter Property="Margin" Value="4" />
    </Style>

    <!-- Style pour les titres de section -->
    <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="18" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Margin" Value="0,0,0,12" />
    </Style>

    <!-- Animation de survol pour les cards -->
    <Style x:Key="AnimatedCardStyle" TargetType="Border">
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1" ScaleY="1" />
            </Setter.Value>
        </Setter>
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleX)"
                                             To="1.02"
                                             Duration="0:0:0.2" />
                            <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleY)"
                                             To="1.02"
                                             Duration="0:0:0.2" />
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleX)"
                                             To="1"
                                             Duration="0:0:0.2" />
                            <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleY)"
                                             To="1"
                                             Duration="0:0:0.2" />
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>